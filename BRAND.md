# Fenix Eval 品牌规格

这是内部品牌规格。前台、后台、README、图表和错误文案按本文对齐。产品事实以 `docs/eval-system-requirements-product.md`、`docs/eval-system-requirements.md`、`docs/eval-display-server-design.md` 为准。本文不改契约，不编造分数。

语气：**严谨，但不晦涩。** 排版仍是学术、锐利、严格。正文怎么写，见第 4 节。

---

## 1. 名称

**对外产品名：Fenix Eval**（两词、首字母大写、中间空格）。

中文里直接写 **Fenix Eval**，不要译成「凤凰评测」「飞尼克斯」。不要单独用「Fenix」指这个产品。

长文档里第一次出现，可以括注功能通称：

> Fenix Eval（评测展示服务）

「评测展示服务」可以和产品名一起用，不能互相替换。

| 位置 | 写法 |
| --- | --- |
| 产品名 | `Fenix Eval` |
| 前台页标题 | `Fenix Eval`；子页可加视图：`Fenix Eval · 对照` |
| 前台页眉 brand | `Fenix Eval` |
| 后台页标题 / 页眉 | `Fenix Eval · 后台` |
| 前台页脚署名 | 先写 `Fenix Eval`，再写不混榜、未报告用量。不要写成 Harbor Hub 或官方榜 |
| 进程日志前缀 | `fenix-eval`（小写、连字符），例如 `fenix-eval listening on :8080` |
| 代码标识符 | `eval-display`、`EVAL_DISPLAY_*`、Go module `evo-harness` **不要改**；品牌名不要倒灌进环境变量或 Harbor 路径 |

不要用这些当界面名：`FenixEval`、`FENIX EVAL`、`Fenix-Eval`。也不要把 `eval-display` 当作用户可见产品名。

---

## 2. 一句话身份

Fenix Eval 收录已经跑完的 Harbor 作业，按 `job_id` 列出我方榜、完成度和用量。

完成度 = 该作业已入库 trial 的 `reward` 均值（`reward` 只有 0 或 1）。用量 = token、成本、步数这五个字段，接口里始终出现。

给谁用：技术组长和评测执行。用来判断私有模型接入 AOS 之后，问题在 AOS 还是客户侧。也可以给 COS 优化和 AOS 引擎背书提供可引用的对照。

**不是**官方 DeepSWE 排名。

产品要先回答三件事（实现细节见需求文档）：接到 AOS 后分不清是 AOS 还是客户侧；资源预估没有测试标准；COS 缺少优化目标、AOS 引擎缺少质量背书。第 1 层先做到：**有 job/trial id 可查、Pass@1 可复算、用量字段始终出现**。

Harbor JSON 里没有的东西，界面不要猜：GPU × 引擎消耗、COS 档位、AOS 编排路径。不要发明 token、cost、步数、ROI 或 COS SKU。

---

## 3. 相对 Harbor / Pier / DeepSWE 的位置

Harbor 作业和官方 Pier / DeepSWE 榜 **不可比**。不要把这边的完成度写成那边的成绩。

| 名称 | 它是什么 | 本产品怎么叫 |
| --- | --- | --- |
| Harbor | 评测 runner 和 TrialResult 契约；官方 CLI 负责执行和 `upload` | 就叫「Harbor」。本服务是兼容上传入口，**不是**第二套 runner，不调用 `harbor run`，不复刻 Hub |
| Pier | 一种执行器。对照包由 Pier 产出 Harbor 目录布局 | 写在 overlay 的 runner 字段里。不要把 Pier 说成本产品 |
| DeepSWE 官网榜 | Pier + `mini-swe-agent` + Modal，另一套执行协议 | **不可比**。Harbor / Peri / COS 作业的完成度不能写成该榜成绩 |
| Fenix Eval | 我方结果面：ingest、S3 作业树、SQLite 分析库、公开读接口、后台 overlay | 「我方榜」。分数由本服务按已入库 trial 自算 |

对照包 `deepswe-peri-3142` 只用来验收 ingest。其中 69/113、20/44 是该包 trial 的实测聚合，不是产品承诺，也不是 Pier 官方榜。

对外引用要能指回：dataset ref（有则写）、agent、model / 端点档位、n、Harbor 或执行器版本、`job_id` / `trial_id`，并注明不是 Pier 官方算法。overlay 的 `incomparability` 用来写清为什么不可比，不用来抬高分数。

---

## 4. 语气

官方语气：**严谨，但不晦涩。**

**严谨**

- 用书面语。不推销，不口号，不用「赋能」「一站式」「领先」。
- 术语固定：完成度、未报告用量、榜单、harness。不要用近义替换（见第 6 节）。
- 缺省填的 0 不要写成测得 0。未报告就写未报告；未签字就写 unsigned；不可比就写不可比。
- Harbor 作业与官方 Pier / DeepSWE 榜不可比。禁止混榜：`peri-3142-full` 和 fail44 必须分开。
- 数字要写清怎么算：完成度的分母是已入库 trial；榜单均值是各作业完成度的算术平均，不是把 trial 混在一起再算。

**不晦涩**

- 句子短。先说结论，再补限定。
- 必要术语第一次出现，用一句人话解释。例如：完成度 = 该作业已入库 trial 的 reward 均值。
- 少用「之 / 其 / 所谓」和绕弯的长从句。不要为了学术腔把句子写长。
- 图表和表的规则写成「要这样做 / 不要那样」，不要写成论文摘要。
- 规格板和界面：**先图后解释。** 读者先看见样子，再读规则。宽屏用双栏：图在左，说明在右。不要把示意图埋在条文后面，也不要让右栏空着。

用户可见错误：`/v1` 和后台用能读懂的书面语，例如「后台令牌无效或未填写。」Harbor 兼容面保留 CLI 依赖的英文标识（`PGRST202`、`PGRST301`、`already exists`）。不要用「Oops」「请稍后重试一下哦」。

后台删除：直接写将删除分析行、兼容记录和对象存储前缀，且不可恢复。不要说软。

代码注释可用中文或英文。产品正文、设计说明和界面文案用上述语气。

---

## 5. 视觉

调性：**学术、锐利、严格、可视化。** 这是排版，不是让文案掉书袋。

要这样做：规格体、编号章节、图有题注、色票用表；直角（半径 0–2px）、细线、1px 浅灰网格；蓝只做切边（顶栏细条、选中标签、下划线、左边线）。

不要那样：软阴影、模糊、半透明蓝铺底、圆角糖果控件、营销口号。

**主题色**是蓝 `#1b4f9a`（`--accent`）。页底是白（`#ffffff`），灰做辅助文字和分割。不要把整页铺成蓝色。绿色 `#2f9e44` 只用于散点「右上更优」的底色（更高完成度、更省消耗），不是品牌主色。朱红 `#c23b22` 已停用。本期不要 logo 文件。名称来自 Fenix，界面不要画凤凰或火焰。

### 5.1 前台（默认）

| 角色 | 变量 | 色值 | 用途 |
| --- | --- | --- | --- |
| 主题色 | `--accent` | `#1b4f9a` | 切边：顶栏细条、选中标签、名称中的 Eval、链接、我方 overlay 字段、`未报告` 标记。大色块见图 2 |
| 页底 | `--bg` / `--paper` | `#ffffff` | 页面与卡片（白底加灰线） |
| 辅助底 | `--surface` | `#f3f5f7` | 弱底、次级表面 |
| 正文 | `--ink` | `#2c333c` | 正文 |
| 辅助文字 | `--muted` / `--faint` | `#6a727c` / `#8b929c` | 说明、标签、轴文字 |
| 分割 | `--line` / `--line-strong` | `#e2e5e9` / `#c9ced6` | 线 |
| 图表语义 | `--ok` | `#2f5d3a` | trial 通过字色 |
| 图表语义 | 更优区 | `#2f9e44`，约 12% 不透明 | 散点右上角的底。效率语义，不是品牌主色 |

栏宽约 `1120px`，水平居中。正文字体：`"Avenir Next", "Helvetica Neue", "PingFang SC", "Noto Sans SC", sans-serif`。标题：`"Iowan Old Style", Palatino, "Songti SC", serif`。数字用 `tabular-nums`。

### 5.2 散点与 harness 色

完整规则见第 8 节。这里只记颜色和坐标。

- 纵轴：**完成度**（向上更高）。
- 横轴：step / cost / token。原始值向右更少，所以右上更好。
- 每个点是一对 **model · harness**。harness = 执行这道题的 agent（字段 `agent_name`）。颜色按 harness 分，不按完成度渐变。
- 调色板（循环）：`#2f9e44` `#f76707` `#7950f2` `#1c7ed6` `#e03131` `#0c8599` `#ae3ec9` `#7048e8`。第一色是绿，但图例仍表示 harness，不表示「更好」。不要把主题蓝 `#1b4f9a` 加入点色。
- 未报告用量：空心方点，落在横轴 0（最右侧）。不要当成测得零消耗。详情只在悬停时出现。禁止混榜。

作业表见第 9 节。

### 5.3 后台

和前台一样：白底灰线。顶栏用深底 `#162033`，表示这里能写。强调色仍是 `#1b4f9a`。删除用 `#9b2c2c`。不要把主题蓝用在「删除」按钮上。

### 5.4 深色主题

本期不做。若以后做：白底改深底，品牌蓝和更优区绿仍要认得出来。不要做成纯黑营销风。

---

## 6. 界面用语

按下表用词。Harbor / PostgREST 字段名在技术列和 API 里保持原文。

用量五个字段始终列出：`n_input_tokens`、`n_cache_tokens`、`n_output_tokens`、`cost_usd`、`n_agent_steps`。

| 用 | 人话 | 不用 |
| --- | --- | --- |
| 完成度 | 该作业已入库 trial 的 reward 均值。图上是纵轴。也可写 `Pass@1 = n_reward_1 / n_trials` | 「效果」「能力」「表现分」 |
| Pass@1 | 和完成度同一个意思；`reward` 只有 0 或 1 | 官方榜 Pass@1；把不同作业的 trial 混在一起再算 |
| 榜单 | 按作业名（`job_name`）分开的一页；一行一个 `job_id` | 用 leaderboard 当中文标题；把多份作业合成一条榜 |
| 对照 | 两个 job 并排看 | 「PK」「对决」 |
| harness | 执行这道题的 agent，字段是 `agent_name` | 用对照包里为 null 的 `config.agent.name` 当身份 |
| agent / model | 身份列；以 `agent_info` / `model_info` 为准 | 用 COS 档位名冒充 model |
| 未报告 / 未报告用量 | 源没给用量（`usage_reported=false`）。库里的 0 是填空 | 「测得 0」「零消耗」「零成本」 |
| 用量 | 上面五个字段，一个都不能少 | 省略字段；用 peri.txt 或 overlay 冒充步数 |
| 作业 / trial | Harbor 的 job / trial | 「实验」「session」 |
| 签字 / unsigned | overlay 里的 `attestation` | 「已认证官方」 |
| 不可比说明 | overlay 的 `incomparability` | 删掉这一行，让它看起来像官方榜 |
| 前台可见 | overlay 的 `listed` | 「发布到官网」 |
| H 型 / M 型 | harness 对照 vs 模型/部署档对照；`job_type` 可空 | 没声明就把两次 run 的差值算在 COS 优化上 |
| 后台 | `/admin/`：overlay、健康、删除 | 「控制台运营中心」 |

榜单名称取自作业名。`peri-3142-full` 与 `peri-3142-fail44`（及 `peri-3142-fail44-v4flash`）必须分开，禁止混榜。

v1 页脚和空态要写明：不提供 peri.txt / ATIF 会话查看。

---

## 7. 做 / 不做

**做**

- 用户可见名称用 Fenix Eval。契约说明仍写 Harbor 兼容、S3/RustFS、SQLite。
- 文案严谨但不晦涩：术语固定，句子短，必要术语先用人话解释。
- 图和文案一起说清怎么算：完成度、横轴向右更少、空心点是未报告、不混榜。规格板先图后解释。图和表分别按第 8、9 节做。
- 用量五个字段始终出现。未报告和测得的 0 用 `usage_reported` 区分。
- 引用数字时带 id，并写清不可比。
- 后台文案区分「尚未 finalize / 前台不可见」和「已在榜」。

**不做**

- 自称官方 Pier / DeepSWE 榜，或把 Harbor 作业和官网榜排在一起。
- 发明 token、cost、步数、ROI，或用其它 job 均值、`tool_use_events_unverified`、草稿 HTML 里的示意值去填。
- 发明 COS SKU / 档位名、GPU 消耗、引擎质量评语。
- 把 peri-3142-full 和 fail44 合成一行，或把 smoke 子集写成全量。
- 用「效果」代替完成度；把缺省 0 写成测得零消耗。
- 营销口吻、吉祥物、为品牌去改 `EVAL_DISPLAY_*` 或 Harbor 路径。
- 把 `docs/eval-panel-draft.html` 里的示意 cost/token/COS/chat 当真。
- 点旁常驻分数气泡、糖果阴影、斑马纹表、用品牌蓝给点上色、把绿当主色。
- 把 `BRAND.html` 里的示意图点位或表行当成 Harbor / Fenix 实测或官方榜。

---

## 8. 图表

首页散点叫「完成度对照」。以后同类型的图也按本节做。颜色见 5.2。主题蓝只用在选中、链接、焦点，不要拿来给点上色。

**先看图 6、图 7（示意），再读下面的规则。** 点是编的。不要写成 Harbor / Fenix 实测，不要抄 Pier / DeepSWE 官方榜或对照包 69/113、20/44，不要发明 COS SKU。线上数据以入库值为准。

### 8.1 示意图数据（只给规格板用）

下面三行只用于图 6、图 7、图 8。作业名以 `demo-` 开头。同一榜 `demo-board-a`，不跟别的榜画在一起。这些数字是编的。

| job_id | 作业名 | harness | model | 完成度 | n_trials | n_agent_steps | cost_usd | token 合计 | usage_reported |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| demo-job-01 | demo-run-flash | demo-harness-a | demo-model-a | 31/50 · 62.0% | 50 | 28 | 1.25 | 15900 | true |
| demo-job-02 | demo-run-chat | demo-harness-b | demo-model-b | 24/50 · 48.0% | 50 | 55 | 2.10 | 22800 | true |
| demo-job-03 | demo-run-null-usage | demo-harness-a | demo-model-c | 8/20 · 40.0% | 20 | 0 | 0 | 0 | false |

token 拆开（仍是示意）：demo-job-01 为 12000 / 800 / 3100；demo-job-02 为 18600 / 0 / 4200；demo-job-03 为 0 / 0 / 0。颜色：`demo-harness-a` → `#2f9e44`，`demo-harness-b` → `#f76707`。

### 8.2 编码

**要这样做**

1. **纵轴是完成度。** 向上更高。线性轴，范围 0–1，刻度写成 0% / 25% / 50% / 75% / 100%。
2. **横轴是 step、cost 或 token，三选一，方向反过来。** 原始值向右更少，所以右上更好（完成度更高、消耗更省）。step 用 `n_agent_steps`；cost 用 `cost_usd`；token 用三个 token 字段相加。轴标题写成「步数（向右更少）」这类。
3. **颜色表示 harness，一个点表示一对 model + harness。** 同一个 harness 同色。一条已完成作业一个点。如果多条作业的 model · harness 相同，悬停时加上作业名，以免分不清。
4. **详情只在悬停时出现。** 指针离开就收起。
5. **未报告用量画空心方点。** 落在横轴 0（反向轴上是最右边）。旁边写「未报告用量」。库里的 0 是填空，不是测得的零消耗。实心点只用于已经报告用量的作业。
6. **一图只画当前榜单。** `peri-3142-full` 和 fail44 必须分开画。

**不要那样**

- 不要用「效果」当轴名。
- 不要按完成度做颜色渐变，也不要用主题蓝给点上色。
- 不要在点旁边常驻分数气泡或引线。
- 不要把空心点当成「测得 0」。
- 不要把不同榜画进同一张图。不要用点的大小表示 `n_trials`。不要加趋势线、气泡图或动画。

### 8.3 样子

**要这样做**

1. **方点、硬边。** 边长 6–8px 的直角方点。悬停时略放大即可。
2. **1px 浅灰实线网格**（`#e2e5e9`）。轴线用 `#c9ced6`。
3. **更优区：** 只在图的右上约 38% × 42% 铺一层 `#2f9e44`、约 12% 透明。区内可写「右上更优」（字色 `#2f5d3a`）。
4. **主题蓝只用在控件：** X 轴分段的选中态、焦点框、链接。轴标题、刻度、网格、点填充都不用它。点色只用 5.2 的八色。其中 `#1c7ed6` 是系列色，不是主题色。
5. **两轴都是线性。** Y 五个百分数刻度。X 4–6 个刻度，必须包含 0（最右边）。数字用 `tabular-nums`，颜色用辅助灰。
6. **图例放在图外右侧**（窄屏放到图下）。关掉 Chart.js 自带 legend，避免挡住更优区。图例：色块 + harness 名；再加一行空心方点 = 未报告用量。规格板上的散点示意图按标本尺寸：绘图区不超过约 460px，不要拉满栏宽。
7. **悬停卡片：白底、1px 灰边、没有阴影、没有圆角。** 标题写「详情」。第一行是 `model · harness`。下面列出：榜单、harness、model、完成度（百分数和 31/50 这种分数）、trial 数、当前 X 值、用量五个字段。未报告时写：「未报告用量：下面的 0 是源没给，不是测得的 0。」卡片跟着指针，出界就收回图内。
8. **X 轴分段：** `step` / `cost` / `token`。选中态和规格板标签一样：蓝底白字。这是控件，不是点的颜色。

**不要那样**

- 不要软阴影、光晕、圆角糖果点、虚线仪表盘网格。
- 不要用品牌蓝铺更优区。
- 不要给图例铺主题蓝底。
- 本榜没有作业时：写一句空态即可，不要画一套空坐标轴做装饰。

图下用一小段说明：token 怎么加总、横轴向右更少、空心点是未报告、不混榜。

---

## 9. 表格

前台作业表和后台列表按本节做。表是用来读数的，不是仪表盘卡片。白底、灰线、紧凑、直角。蓝只用在可点击的地方：链接、选中标签、焦点、选中行的左边线。

**先看图 8、图 9（示意），再读下面的规则。** 行和图 6 是同一套编出来的 `demo-` 作业。不要当成实测榜。示意数据见 8.1。

### 9.1 列

**要这样做**

前台作业表列和接口字段对齐，用量列不能少：

| 列标题 | 字段 | 对齐 |
| --- | --- | --- |
| 对照 | 勾选两条作业做对照。表头写「对照」。直角方框，与作业名第一行对齐；左侧留空，不贴选中边线。 | 中 / 左 |
| 作业 | `job_name`，下面附 `job_id` | 左 |
| harness | `agent_name` | 左 |
| model | `model_name` | 左 |
| 完成度 | 分数（如 31/50）和百分数 | 右 |
| n_trials | trial 数 | 右 |
| n_input_tokens 等五列 | 用量，始终出现 | 右 |
| 签字 | `attestation_status` | 左 |
| 执行器 | runner 名 / 版本 | 左 |

未报告用量不要单开一列：数字仍显示 0，格子里标「未报告」。

对照勾选、签字、执行器是前台作业表的现有列，规格板示意图必须带上。不要用完成度进度条。

后台可以再加：ingest、前台可见、签字、Pass@1、hub / 分析 trial、`job_type`、`endpoint_class`、操作。删除按钮用 `#9b2c2c`，不用主题蓝。

一行一个 `job_id`。表只含当前榜单。**榜单名称只出现在页顶标签，不要作为表的第一列每行重复。**

**不要那样**

- 不要省略用量列。
- 不要把 `peri-3142-full` 和 fail44 合成一张表。
- 完成度列不要用粗进度条抢视线。若留一条细线，高度 2px、用分割灰，不要铺主题蓝。

### 9.2 样子

**要这样做**

1. **表头浅灰、底边略深（`#c9ced6`）。** 表体行间 1px `#e2e5e9`。数字用 `tabular-nums`。单元格紧凑。白底、直角、没有卡片阴影。
2. **文本左齐，数字右齐。** 表头和列同一方向。
3. **悬停行用极浅灰 `#f3f5f7`。** 选中行同样浅底，再加 2px 主题蓝左边线。焦点框 1px 主题蓝。作业名可以当链接，颜色 `#1b4f9a`。
4. **未报告的用量格：仍显示 0，并标「未报告」（主题蓝）。** 源真的报告了 0，就只显示 0，不要加「未报告」。
5. **空态写一句人话，居中、灰色、不要插画。** 前台：「还没有已完成且未隐藏的作业。请用 Harbor CLI 上传后再刷新。」后台：「还没有 hub_job。」v1 可以注明不提供 peri.txt / ATIF 会话查看。
6. **表太宽就横向滚动。** 粘住的表头仍是白底。

**不要那样**

- 不要斑马纹（隔行上色容易看起来像仪表盘）。
- 不要用半透明品牌蓝铺行。
- 未报告时不要删列、不要改成「—」、不要写成「测得 0」或「零消耗」。
- 不要用空表头充数。不要给冻结列上彩色。

对照页的并排表、trial 表用同一套：细线、数字右齐、悬停浅灰、选中左边线。
